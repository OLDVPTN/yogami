<article class="testimonial-card">
  <a class="media" href="/@<%= item.username %>">
    <% if (item.mediaType === 'video') { %>
      <video src="<%= mediaDisplayUrl(item) %>" controls preload="metadata"></video>
    <% } else { %>
      <img src="<%= mediaDisplayUrl(item) %>" alt="Testimoni dari @<%= item.username %>" loading="lazy">
    <% } %>
  </a>
  <div class="content">
    <a class="author" href="/@<%= item.username %>">@<%= item.username %></a>
    <p><%= item.text %></p>
    <div class="keywords">
      <% (item.keywords || []).slice(0, 1).forEach((key) => { %>
        <a href="/search?q=<%= encodeURIComponent(key) %>">#<%= key %></a>
      <% }) %>
    </div>
    <time><%= formatDateTime(item.createdAt, config.timezone) %></time>
  </div>
</article>
